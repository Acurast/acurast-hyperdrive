{
  "name": "ibcf-protocol-monorepo",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@ethereumjs/trie": "5.0.1",
    "@openzeppelin/contracts": "4.8.0",
    "@truffle/hdwallet-provider": "2.1.1",
    "@typescript-eslint/eslint-plugin": "5.42.1",
    "@typescript-eslint/parser": "5.42.1",
    "eslint": "8.27.0",
    "eslint-config-prettier": "8.5.0",
    "eslint-plugin-prettier": "4.2.1",
    "prettier": "2.8.1",
    "truffle": "5.6.5",
    "web3": "1.8.1",
    "ec-pem": "0.18.0"
  },
  "scripts": {
    "compile": "truffle compile",
    "test": "truffle test --migrations_directory migrations_null",
    "deploy": "truffle deploy --network goerli --reset",
    "build": "yarn workspaces foreach --verbose run build",
    "lint:check": "yarn workspaces foreach --verbose run lint .",
    "lint:fix": "yarn workspaces foreach --verbose run lint --fix .",
    "prettier:check": "yarn workspaces foreach --verbose run prettier --check .",
    "prettier:fix": "yarn workspaces foreach --verbose run prettier --write .",
    "fmt": "yarn lint:fix && yarn prettier:fix"
  },
  "workspaces": [
    "packages/ibcf-sdk",
    "packages/root-transmitter"
  ],
  "packageManager": "yarn@3.3.0"
}
